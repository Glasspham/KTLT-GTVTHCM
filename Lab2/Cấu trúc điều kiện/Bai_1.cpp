#include<iostream>
using namespace std;
int main()
{
    int n;
    cout << "Nhap mot so bat ki: ";
    cin >> n;
    if(n % 2 == 0) cout << "so chan";
    else cout << "so le";
    return 0;
}